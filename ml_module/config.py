"""
Конфигурационный файл для системы сбора и анализа данных матчей Dota 2.

Содержит все настройки для:
 - Подключения к базам данных и внешним API
 - Параметров сбора и фильтрации матчей
 - Алгоритмов определения ролей и детекции руинеров
 - Настроек логирования и отладки
"""

import os
from dotenv import load_dotenv

# Загружаем переменные окружения из .env файла
# .env должен находиться в корневой директории проекта
# Это позволяет хранить секретные данные (пароли, API ключи) отдельно от кода
load_dotenv(dotenv_path=os.path.join(os.path.dirname(__file__), '..', '.env'))

# === КОНФИГУРАЦИЯ БАЗЫ ДАННЫХ ===
# Все значения загружаются из переменных окружения для безопасности

DB_USER = os.getenv('DB_USER')          # Имя пользователя БД
DB_PASSWORD = os.getenv('DB_PASSWORD')  # Пароль пользователя БД
DB_HOST = os.getenv('DB_HOST')          # Хост БД (обычно localhost или IP адрес)
DB_PORT = os.getenv('DB_PORT')          # Порт БД (обычно 5432 для PostgreSQL)

# Названия отдельных баз данных для разделения функциональности
DATASET_DB_NAME = os.getenv('DATASET_DB_NAME')  # БД для хранения матчей и игроков (основная)
HEROES_DB_NAME = os.getenv('HEROES_DB_NAME')    # БД для справочной информации о героях

# Строки подключения к базам данных в формате PostgreSQL URI
DATASET_DATABASE_URL = f"postgresql://{DB_USER}:{DB_PASSWORD}@{DB_HOST}:{DB_PORT}/{DATASET_DB_NAME}"
HEROES_DATABASE_URL = f"postgresql://{DB_USER}:{DB_PASSWORD}@{DB_HOST}:{DB_PORT}/{HEROES_DB_NAME}"

# === КОНФИГУРАЦИЯ STEAM API ===
# Ключ Steam API (получается бесплатно на steamcommunity.com/dev)
# КРИТИЧЕСКИ ВАЖНО: без валидного ключа сбор данных невозможен
STEAM_API_KEY = os.getenv('STEAM_API_KEY')

# URL эндпоинта Steam API для получения истории матчей по sequence number
# Используется метод GetMatchHistoryBySequenceNum для последовательного сбора данных
STEAM_API_MATCH_HISTORY_URL = "https://api.steampowered.com/IDOTA2Match_570/GetMatchHistoryBySequenceNum/V001/"

# API для получения информации о героях (используется для инициализации БД HEROES)
# OpenDota предоставляет бесплатный доступ к справочной информации
OPENDOTA_HEROES_API = "https://api.opendota.com/api/heroes"

# Параметры сбора данных - определяют объем и скорость сбора
STARTING_MATCH_SEQUENCE_NUMBER = 7080800000  # Стартовый sequence number для начала сбора
                                             # Этот номер должен соответствовать началу обновления (patch) в Dota 2

TARGET_MATCHES_COUNT = 5_000_000             # Целевое количество матчей для сбора (5 миллионов)
                                             # Достаточно для статистически значимого анализа

DATABASE_SAVE_CHUNK_SIZE = 10_000            # Размер чанка для пакетного сохранения в БД
                                             # Оптимальный диапазон: 10,000-25,000 записей

BURST_TIME_TIMESTAMP = 1755968400            # Unix timestamp - матчи меньше этого времени игнорируются
                                             # Используется для фильтрации старых данных

MATCHES_PER_API_REQUEST = 100                # Количество матчей, запрашиваемых за один API вызов
                                             # Максимум: 100 (ограничение Steam API)

API_REQUEST_DELAY = 4                        # Задержка между запросами к Steam API в секундах
                                             # Необходимо для соблюдения rate limit и стабильности

MAX_CONSECUTIVE_INCOMPLETE_RESPONSES = 5     # Максимальное количество неполных ответов от API
                                             # После превышения программа остановится

# === НАСТРОЙКИ ЛОГИРОВАНИЯ (DEBUG) ===
ENABLE_DETAILED_STATISTICS = False      # Детальная статистика обработки каждого API вызова
ENABLE_RUINER_LOGGING = False           # Подробное логирование процесса детекции руинеров
ENABLE_ROLE_LOGGING = False             # Логирование процесса назначения ролей игрокам

# === ПАРАМЕТРЫ ВАЛИДАЦИИ ДАННЫХ ===
# Обязательные поля для матчей
REQUIRED_MATCH_FIELDS = {
    "match_id",                   # Уникальный идентификатор матча (64-bit integer)
    "match_seq_num",              # Последовательный номер матча для API запросов
    "radiant_win",                # Победила ли команда Radiant (True/False)
    "duration",                   # Длительность матча в секундах
    "start_time",                 # Unix timestamp начала матча
    "tower_status_radiant",       # Битовая маска состояния башен команды Radiant
    "tower_status_dire",          # Битовая маска состояния башен команды Dire
    "barracks_status_radiant",    # Битовая маска состояния казарм команды Radiant
    "barracks_status_dire",       # Битовая маска состояния казарм команды Dire
    "lobby_type",                 # Тип лобби (публичный, рейтинговый, приватный и т.д.)
    "game_mode",                  # Игровой режим (All Pick, Captain's Mode, Random Draft и т.д.)
    "radiant_score",              # Количество убийств команды Radiant
    "dire_score",                 # Количество убийств команды Dire
    "players"                     # Список игроков матча (массив из 10 элементов)
}

# Обязательные поля для игроков
REQUIRED_PLAYER_FIELDS = {
    "account_id",       # Уникальный ID аккаунта игрока Steam (может быть анонимным)
    "hero_id",          # ID выбранного героя (числовой идентификатор)
    "hero_variant",     # Вариант героя (1 - 6)
    "team_number",      # Номер команды (0 = Radiant, 1 = Dire)
    "net_worth",        # Общая стоимость предметов игрока на конец матча
    "last_hits",        # Количество добитых крипов (основной показатель фарма)
    "denies",           # Количество заблокированных союзных крипов
    "gold_per_min",     # Среднее золото в минуту за весь матч
    "xp_per_min",       # Средний опыт в минуту за весь матч

    # Предметы в основных слотах (6 основных слотов инвентаря)
    "item_0",           # Предмет в слоте 0 (верхний левый)
    "item_1",           # Предмет в слоте 1 (верхний средний)
    "item_2",           # Предмет в слоте 2 (верхний правый)
    "item_3",           # Предмет в слоте 3 (нижний левый)
    "item_4",           # Предмет в слоте 4 (нижний средний)
    "item_5",           # Предмет в слоте 5 (нижний правый)

    # Предметы в рюкзаке (дополнительное хранилище)
    "backpack_0",       # Предмет в рюкзаке слот 0
    "backpack_1",       # Предмет в рюкзаке слот 1
    "backpack_2",       # Предмет в рюкзаке слот 2

    # Боевая статистика
    "kills",            # Количество убийств героев противника
    "deaths",           # Количество смертей
    "assists",          # Количество помощи в убийствах (ассисты)

    # Дополнительные предметы и характеристики
    "item_neutral",     # Основной нейтральный предмет
    "item_neutral2",    # Дополнительный нейтральный предмет
    "level",            # Уровень героя на конец игры (1-30)
    "aghanims_scepter", # Есть ли Aghanim's Scepter (1/0)
    "aghanims_shard",   # Есть ли Aghanim's Shard (1/0)
    "moonshard"         # Есть ли съеденный Moon Shard (1/0)
}

# Разрешенные игровые режимы для анализа
ALLOWED_GAME_MODES = {
    3,   # Random Draft - каждый игрок выбирает из ограниченного пула героев
    4,   # Single Draft - каждому игроку доступны 3 случайных героя
    5,   # All Random - полностью случайный выбор героев
    22   # All Pick (Ranked) - рейтинговые матчи, свободный выбор героев
}

# Разрешенные типы лобби
ALLOWED_LOBBY_TYPES = {
    0,   # Public matchmaking - обычные публичные матчи
    7    # Ranked matchmaking - рейтинговые матчи
}

# Дополнительные параметры фильтрации матчей
MINIMUM_MATCH_DURATION = 1200           # Минимальная длительность матча в секундах (20 минут)
                                        # Исключает сдачи на раннем этапе и подставные игры

NUMBER_OF_PLAYERS_PER_TEAM = 5          # Ожидаемое количество игроков в каждой команде
                                        # Стандарт для Dota 2: 5 против 5

# Пороговые значения для обнаружения "мёртвых" матчей
# Помогают исключить матчи с большим количеством ботов или афк игроков
MAX_LEVEL_1_RATIO = 0.1                 # Максимальная доля игроков на 1 уровне (10%)
MAX_ZERO_LASTHITS_RATIO = 0.1           # Максимальная доля игроков с 0 last_hits (10%)
MIN_TOTAL_LAST_HITS = 30                # Минимальное суммарное количество last_hits на весь матч

# === КОНФИГУРАЦИЯ АЛГОРИТМА ОПРЕДЕЛЕНИЯ РОЛЕЙ ===
# ID предметов, характерных для роли поддержки (support)
SUPPORT_ITEM_IDS = {
    37,    # Ghost Scepter
    43,    # Sentry Ward
    102,   # Force Staff
    214,   # Tranquil Boots
    218,   # Observer and Sentry Wards
    229,   # Solar Crest
    254,   # Glimmer Cape
    256,   # Aeon Disk
    269,   # Holy Locket
    931,   # Boots of Bearing
    1128   # Pavise
}

# Исключения для определения support предметов у конкретных героев
# Некоторые герои могут покупать support предметы в роли carry
# Формат: {hero_id: {set_of_item_ids_to_ignore}}
HERO_ITEM_EXCEPTIONS = {
    97: {102},  # Magnus (ID: 97) - Force Staff
}

# Исключения для расчета support_score
# Множество содержит героев (или их варианты), которые зачастую играются как саппорты
# Множество содержит (hero_id, hero_variant) или просто hero_id для всех вариантов
HERO_SUPPORT_SCORE_EXCEPTIONS = {
    (73, 3)  # Alchemist variant 3
}

# Веса для расчета support_score
# Определяют важность различных факторов при определении роли
SUPPORT_ITEMS_WEIGHT = 5                    # Вес количества саппорт предметов
SUPPORT_SCORE_EXCEPTION_WEIGHT = 10         # Фиксированное значение для исключений из HERO_SUPPORT_SCORE_EXCEPTIONS
NET_WORTH_WEIGHT = 10                       # Вес net worth (инвертированный)
LAST_HITS_WEIGHT = 5                        # Вес last hits (инвертированный)
GPM_WEIGHT = 5                              # Вес GPM (инвертированный)
XPM_WEIGHT = 5                              # Вес XPM (инвертированный)

# === ПАРАМЕТРЫ ДЕТЕКЦИИ РУИНЕРОВ ===
# Ожидаемые KDA значения для расчета отклонений
# Используются для определения аномально низкой эффективности игрока
CORE_EXPECTED_KDA = 4.5                     # Ожидаемый KDA для core игроков
CORE_KDA_TOLERANCE = 4.0                    # Допустимое отклонение KDA для core
SUPPORT_EXPECTED_KDA = 3.3                  # Ожидаемый KDA для support игроков
SUPPORT_KDA_TOLERANCE = 2.8                 # Допустимое отклонение KDA для support

# Параметры расчета ожидаемого GPM (Gold Per Minute)
# Используются для оценки экономической эффективности игрока
INCOME_MINIMUM_THRESHOLD = 0.3              # Минимальный процент от ожидаемого дохода (30%)
GPM_CALCULATION_START_MINUTE = 20           # Минута, с которой начинается рост ожидаемого GPM

# Параметры GPM для core игроков
CORE_BASE_GPM = 500                         # Базовый GPM для core игроков
CORE_GPM_GROWTH_RATE = 6.0                  # Скорость роста GPM (за минуту после 20-й минуты)
CORE_MAX_GPM = 800                          # Максимальный ожидаемый GPM для core

# Параметры GPM для support игроков
SUPPORT_BASE_GPM = 300                      # Базовый GPM для support игроков
SUPPORT_GPM_GROWTH_RATE = 5.0               # Скорость роста GPM у support
SUPPORT_MAX_GPM = 600                       # Максимальный ожидаемый GPM для support

# Пороговые значения для классификации руинеров
RUINER_DETECTION_THRESHOLD = 0.6            # Порог ruiner_index для окончательной классификации
                                            # Значения выше этого порога = руинер

RUINER_LOGGING_THRESHOLD = 0.5              # Порог для логирования подозрительных игроков
                                            # Позволяет анализировать пограничные случаи

RUINER_EMPTY_SLOTS_LIMIT = 4                # Максимальное количество пустых слотов предметов
                                            # Больше = автоматически руинер

RUINER_SAME_ITEMS_LIMIT = 4                 # Максимальное количество одинаковых предметов
                                            # Указывает на intentional feeding или trolling

# Веса компонентов в формуле ruiner_index
TEAM_DEATH_RATIO_WEIGHT = 0.25              # Вес компонента "доля смертей от командных"
KDA_SCORE_WEIGHT = 0.25                     # Вес компонента "отклонение KDA от ожидаемого"
INCOME_SCORE_WEIGHT = 0.3                   # Вес компонента "экономические показатели"
CONTRIBUTION_SCORE_WEIGHT = 0.2             # Вес компонента "вклад в убийства команды"

# Исключения для расчета индекса руинера по героям
# Ключ: (hero_id, hero_variant) или просто hero_id для всех вариантов
# Значение: множество компонентов для исключения из расчета
# Возможные компоненты для исключения:
# - 'team_death_ratio': доля смертей игрока от общих смертей вражеской команды
# - 'kda_score': отклонение KDA от ожидаемого для роли
# - 'income_score': экономическая эффективность (GPM, net worth)
# - 'contribution_score': вклад в убийства своей команды
HERO_RUINER_EXCEPTIONS = {
    (73, 3): {'income_score'},  # Alchemist (ID: 73) with variant 3
}

# === КОНСТАНТЫ ДЛЯ АНАЛИЗА ДАТАСЕТА ===
# Параметры для анализа собранных данных и генерации статистики

PRIVATE_ACCOUNT_ID = 4294967295             # Специальный ID для приватных аккаунтов Steam
                                            # Максимальное значение uint32, используется Steam

# Пороги для статистического анализа героев
MINIMUM_GAMES_FOR_HERO_WINRATE = 100        # Минимум матчей для расчета винрейта героя
                                            # Обеспечивает статистическую значимость

MINIMUM_GAMES_FOR_HERO_VARIANT_WINRATE = 50 # Минимум матчей для винрейта вариантов героев
                                            # Arcana/Persona имеют меньше данных

TOP_HEROES_DISPLAY_COUNT = 20               # Количество героев для отображения в топах
                                            # Стандартное количество для рейтингов

# Пороги для предупреждений и анализа качества данных
PRIVATE_ACCOUNTS_WARNING_THRESHOLD = 75     # Процент приватных аккаунтов для предупреждения
                                            # Высокий процент может указывать на проблемы

DUPLICATE_DISPLAY_LIMIT = 10                # Количество дубликатов матчей для отображения
                                            # Ограничивает вывод при анализе дубликатов

# Битовые маски для анализа игровых структур
# Используются для декодирования состояния башен и казарм из Steam API
TOWERS_BITMASK = 0x7FF                      # 11 единиц (0b11111111111) для 11 башен каждой команды
                                            # Каждый бит представляет одну башню

BARRACKS_BITMASK = 0x3F                     # 6 единиц (0b111111) для 6 казарм каждой команды
                                            # Каждый бит представляет одну казарму
